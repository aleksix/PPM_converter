# Netpbm edge detector

## Building
```shell
make
```

### Example of usage
```shell
./ppm_converter -i [filename] -o [filename] [-t] [number of threads]
```
Some example inputs can be found in /example_inputs/